import React from 'react';
import ReactDOM from 'react-dom';
import Bundler from './components/Bundler/Bundler';
import './index.css';


ReactDOM.render(
  <React.StrictMode>
    <Bundler/>
  </React.StrictMode>,
  document.getElementById('root')
);
